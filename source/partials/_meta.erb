<span class="byline author vcard">Posted by <span class="fn"><%= settings.author %></span></span>
on
<time datetime="<%= article.date.xmlschema %>">
  <%= article.date.strftime('%b %e, %Y') %>
</time>
<% if article.categories.size > 0 -%>
in
<%= partial 'partials/post_categories', :locals => { :article => article } %>
<% end %>